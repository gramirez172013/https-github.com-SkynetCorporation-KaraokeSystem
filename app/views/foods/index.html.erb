<p id="notice"><%= notice %></p>

<h1> Pedidos </h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Miembro</th>
      <th>Sala</th>
      <th>Pedido</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.nombre %></td>
        <td><%= food.miembro %></td>
        <td><%= food.sala %></td>
        <td><%= food.pedido %></td>
        <td><%= link_to 'Show', food %></td>
        <td><%= link_to 'Edit', edit_food_path(food) %></td>
        <td><%= link_to 'Destroy', food, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ingresa tu pedido', new_food_path %>
<b> | </b>
<a href="/">Cancel</a>
